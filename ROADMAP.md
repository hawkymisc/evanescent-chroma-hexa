# Evanescent Chroma Hexa - 開発ロードマップ

最終更新: 2025-10-28

## 📊 現状の実装状況

### ✅ Phase 1: MVP（完全実装済み）

すべてのMVP機能が実装完了しています。

- [x] 六角形グリッドの描画（CSS clip-path使用）
- [x] 4色のランダム初期配置（赤、青、緑、黄）
- [x] クリックによるタイル消去（2個以上のグループ）
- [x] スコア計算（`(n-2)²`方式）
- [x] 重力処理（縦列単純落下）
- [x] ゲーム終了判定
- [x] 新規ゲームボタン

### ✅ Phase 2: 機能拡張（部分実装済み）

#### 実装済み
- [x] アニメーション（削除・スライド）
- [x] ハイスコア保存（Cookie、有効期限10年）
- [x] アニメーション速度設定（ちょっぱや・早い・ふつうの3段階）
- [x] レスポンシブデザイン（スマホ対応、768px/400pxブレークポイント）
- [x] ゲーム終了時のハイスコア更新通知

#### 未実装
- [ ] **Undo機能** - 次の優先実装候補
- [ ] **ヒント表示** - 次の優先実装候補
- [ ] **設定画面**（グリッドサイズ、色数）

### ❌ Phase 3: 高度な機能（未実装）

- [ ] ゲームモード選択
  - [ ] クラシック（時間制限なし）
  - [ ] タイムアタック
  - [ ] パズルモード（固定盤面）
- [ ] サウンドエフェクト
- [ ] リーダーボード（オプション）
- [ ] PWA対応（オフラインプレイ）

---

## 🎯 次にやるべきことの優先順位

### 🔴 優先度：高（ユーザー体験の大幅改善）

#### 1. Undo機能の実装

**推定時間**: 3-4時間
**優先理由**: パズルゲームの基本機能。ユーザーが試行錯誤できるようになり、満足度が大幅に向上

**実装内容**:
- ゲーム状態の履歴管理（最大10-20手）
- Undoボタンの追加（UI）
- キーボードショートカット（Ctrl+Z / Cmd+Z）
- 履歴のメモリ管理（古い履歴の削除）

**技術的詳細**:
```javascript
// ゲーム状態に履歴を追加
gameState = {
  tiles: new Map(),
  score: 0,
  gameOver: false,
  isAnimating: false,
  animationSpeed: 'normal',
  history: [] // 追加: 最大20エントリ
}

// 各手の前にスナップショットを保存
function saveSnapshot() {
  const snapshot = {
    tiles: new Map(gameState.tiles),
    score: gameState.score
  };
  gameState.history.push(snapshot);

  // 最大20手まで保持
  if (gameState.history.length > 20) {
    gameState.history.shift();
  }
}

// Undo処理
function undo() {
  if (gameState.history.length === 0) return;

  const snapshot = gameState.history.pop();
  gameState.tiles = new Map(snapshot.tiles);
  gameState.score = snapshot.score;
  gameState.gameOver = false;

  renderGrid();
  updateScore();
}
```

**期待効果**:
- ユーザーの満足度向上
- より戦略的なプレイが可能に
- 初心者の離脱率低下

---

#### 2. ヒント機能の実装

**推定時間**: 2-3時間
**優先理由**: 初心者や行き詰まったユーザーへのサポート。ゲームの継続性向上

**実装内容**:
- 最大グループの検出とハイライト
- ヒントボタンの追加
- 使用回数制限（オプション、1ゲームあたり3回など）
- クールダウン機能（連続使用を防ぐ）

**技術的詳細**:
```javascript
// 最大グループを検出
function findLargestGroup() {
  let largestGroup = [];
  const checked = new Set();

  for (const [key, tile] of gameState.tiles) {
    if (checked.has(key)) continue;

    const group = findGroup(tile.q, tile.r);
    group.forEach(t => checked.add(coordKey(t.q, t.r)));

    if (group.length >= 2 && group.length > largestGroup.length) {
      largestGroup = group;
    }
  }

  return largestGroup;
}

// ヒント表示
function showHint() {
  const largestGroup = findLargestGroup();

  if (largestGroup.length < 2) {
    alert('消せるグループがありません');
    return;
  }

  // グループをハイライト（3秒間）
  largestGroup.forEach(tile => {
    const el = document.querySelector(`[data-q="${tile.q}"][data-r="${tile.r}"]`);
    if (el) el.classList.add('hint-highlight');
  });

  setTimeout(() => {
    document.querySelectorAll('.hint-highlight').forEach(el => {
      el.classList.remove('hint-highlight');
    });
  }, 3000);
}
```

**期待効果**:
- 初心者の学習曲線が緩やかに
- 行き詰まった時の救済策
- ゲームの継続性向上

---

### 🟡 優先度：中（機能拡張）

#### 3. サウンドエフェクトの追加

**推定時間**: 2-3時間
**優先理由**: ゲーム体験の没入感を高める。リピート率向上

**実装内容**:
- タイル消去時の効果音（pop.wav）
- ゲーム終了時の効果音（gameover.wav）
- ハイスコア更新時の効果音（highscore.wav）
- BGM（オプション）
- ON/OFF切り替えボタン
- Web Audio API使用

**技術的詳細**:
```javascript
// サウンドマネージャー
const soundManager = {
  enabled: true,
  sounds: {
    pop: new Audio('assets/sounds/pop.wav'),
    gameOver: new Audio('assets/sounds/gameover.wav'),
    highScore: new Audio('assets/sounds/highscore.wav')
  },

  play(soundName) {
    if (!this.enabled) return;

    const sound = this.sounds[soundName];
    if (sound) {
      sound.currentTime = 0;
      sound.play().catch(() => {
        // ユーザーインタラクション前のエラーを無視
      });
    }
  },

  toggle() {
    this.enabled = !this.enabled;
    // Cookieに保存
    setCookie('soundEnabled', this.enabled);
  }
};
```

**必要なアセット**:
- `assets/sounds/pop.wav`（タイル消去音）
- `assets/sounds/gameover.wav`（ゲーム終了音）
- `assets/sounds/highscore.wav`（新記録音）

**期待効果**:
- ゲームの魅力度向上
- 没入感の増加
- リピート率アップ

---

#### 4. 設定画面の実装

**推定時間**: 4-5時間
**優先理由**: ユーザーの好みに合わせたカスタマイズ。リプレイ性の向上

**実装内容**:
- グリッドサイズ変更（6×7、8×9、10×11）
- 色数変更（3色、4色、5色、6色）
- 難易度プリセット（簡単、普通、難しい）
- 設定の保存（Cookie/LocalStorage）
- 設定モーダルのUI

**技術的詳細**:
```javascript
// 設定オブジェクト
const gameSettings = {
  gridCols: 8,
  gridRows: 9,
  numColors: 4,

  // 設定を保存
  save() {
    setCookie('gridCols', this.gridCols);
    setCookie('gridRows', this.gridRows);
    setCookie('numColors', this.numColors);
  },

  // 設定を読み込み
  load() {
    this.gridCols = parseInt(getCookie('gridCols')) || 8;
    this.gridRows = parseInt(getCookie('gridRows')) || 9;
    this.numColors = parseInt(getCookie('numColors')) || 4;
  }
};

// 難易度プリセット
const DIFFICULTY_PRESETS = {
  easy: { gridCols: 6, gridRows: 7, numColors: 3 },
  normal: { gridCols: 8, gridRows: 9, numColors: 4 },
  hard: { gridCols: 10, gridRows: 11, numColors: 5 }
};
```

**期待効果**:
- リプレイ性の大幅向上
- 幅広いユーザー層への対応
- 長期的なエンゲージメント

---

### 🟢 優先度：低（でも価値が高い）

#### 5. PWA対応（Progressive Web App）

**推定時間**: 3-4時間
**優先理由**: 最小の工数で「アプリ化」を実現。マーケティング的価値が高い

**実装内容**:
- `manifest.json`の作成
- Service Workerの実装
- オフラインキャッシング
- アイコン画像の作成（複数サイズ）
- インストールプロンプトの表示

**技術的詳細**:

**manifest.json**:
```json
{
  "name": "Evanescent Chroma Hexa",
  "short_name": "Hexa Puzzle",
  "description": "六角形グリッドで遊ぶ、色合わせパズルゲーム",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#667eea",
  "theme_color": "#667eea",
  "orientation": "portrait",
  "icons": [
    {
      "src": "icons/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "icons/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

**service-worker.js**:
```javascript
const CACHE_NAME = 'hexa-v1.0.0';
const urlsToCache = [
  '/',
  '/index.html',
  '/style.css',
  '/game.js',
  '/icons/icon-192.png',
  '/icons/icon-512.png'
];

// インストール
self.addEventListener('install', event => {
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then(cache => cache.addAll(urlsToCache))
  );
});

// フェッチ（Cache-first戦略）
self.addEventListener('fetch', event => {
  event.respondWith(
    caches.match(event.request)
      .then(response => response || fetch(event.request))
  );
});

// 古いキャッシュの削除
self.addEventListener('activate', event => {
  event.waitUntil(
    caches.keys().then(cacheNames => {
      return Promise.all(
        cacheNames.map(cacheName => {
          if (cacheName !== CACHE_NAME) {
            return caches.delete(cacheName);
          }
        })
      );
    })
  );
});
```

**index.htmlに追加**:
```html
<head>
  <!-- PWA Manifest -->
  <link rel="manifest" href="manifest.json">
  <meta name="theme-color" content="#667eea">

  <!-- iOS Safari用 -->
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="apple-mobile-web-app-title" content="Hexa Puzzle">
  <link rel="apple-touch-icon" href="icons/icon-192.png">
</head>

<script>
  // Service Worker登録
  if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('/service-worker.js')
      .then(reg => console.log('Service Worker registered'))
      .catch(err => console.error('Service Worker failed', err));
  }
</script>
```

**必要なアセット**:
- `icons/icon-192.png`（192×192px）
- `icons/icon-512.png`（512×512px）
- `icons/favicon.ico`

**期待効果**:
- ホーム画面に追加可能
- オフラインでもプレイ可能
- アプリのような体験
- マーケティング訴求力向上

---

#### 6. タイムアタックモードの実装

**推定時間**: 3-4時間
**優先理由**: 新しい遊び方の提供。競争要素の追加

**実装内容**:
- 制限時間の設定（60秒、120秒、180秒）
- タイマー表示（カウントダウン）
- モード選択画面
- タイムアタック専用のハイスコア
- 時間ボーナス（残り時間 × 10点）

**技術的詳細**:
```javascript
// タイムアタックモード用の状態
const timeAttackState = {
  enabled: false,
  timeLimit: 120, // 秒
  remainingTime: 120,
  timerId: null,

  start() {
    this.enabled = true;
    this.remainingTime = this.timeLimit;

    this.timerId = setInterval(() => {
      this.remainingTime--;
      updateTimerDisplay();

      if (this.remainingTime <= 0) {
        this.stop();
        gameOver();
      }
    }, 1000);
  },

  stop() {
    if (this.timerId) {
      clearInterval(this.timerId);
      this.timerId = null;
    }
  },

  getTimeBonus() {
    return this.remainingTime * 10;
  }
};
```

**期待効果**:
- ゲームの多様性
- 競争要素の追加
- リプレイ性の向上

---

## 📱 ネイティブアプリ化の戦略

### ステップ1: PWA化（最優先・最も現実的） ⭐推奨

**実装時間**: 3-4時間
**コスト**: 実質ゼロ
**難易度**: ★☆☆☆☆

**メリット**:
- 既存コードをほぼそのまま利用
- iOS/Android両対応
- ホーム画面に追加可能
- オフライン動作
- プッシュ通知可能（将来的に）
- App Store審査不要

**デメリット**:
- iOSでは一部機能制限あり
- アプリストアに登録できない

**実装内容**（上記「PWA対応」参照）

---

### ステップ2: Capacitor化（App Store/Google Play配信）

**実装時間**: 6-8時間
**コスト**: 開発者アカウント（Apple $99/年、Google $25買い切り）
**難易度**: ★★★☆☆

**メリット**:
- PWAをベースにネイティブアプリ化
- App Store / Google Playに配信可能
- ネイティブAPI利用可能（振動、センサー、カメラなど）
- Webと同じコードベース（保守性が高い）
- 段階的な移行が可能

**デメリット**:
- 開発者アカウントが必要
- アプリストア審査が必要
- ネイティブ環境のセットアップが必要（Xcode、Android Studio）

**実装手順**:

```bash
# 1. Capacitorのインストール
npm install @capacitor/core @capacitor/cli

# 2. プロジェクト初期化
npx cap init "Evanescent Chroma Hexa" "com.yourname.hexapuzzle"

# 3. iOS/Androidプロジェクトの追加
npx cap add ios
npx cap add android

# 4. ビルド
npm run build  # 既存のファイルをdistにコピー
npx cap sync   # ネイティブプロジェクトに同期

# 5. ネイティブIDEで開く
npx cap open ios      # Xcodeで開く
npx cap open android  # Android Studioで開く
```

**追加で実装できるネイティブ機能**:

```javascript
// 振動フィードバック（タイル消去時）
import { Haptics, ImpactStyle } from '@capacitor/haptics';

async function vibrate() {
  await Haptics.impact({ style: ImpactStyle.Light });
}

// シェア機能（スコアをSNSに共有）
import { Share } from '@capacitor/share';

async function shareScore(score) {
  await Share.share({
    title: 'Evanescent Chroma Hexa',
    text: `スコア${score}点を獲得しました！`,
    url: 'https://hawkymisc.github.io/anescent-chroma-hexa/',
    dialogTitle: 'スコアを共有'
  });
}

// ローカル通知（ハイスコア更新時）
import { LocalNotifications } from '@capacitor/local-notifications';

async function notifyHighScore(score) {
  await LocalNotifications.schedule({
    notifications: [
      {
        title: "新記録達成！",
        body: `${score}点の新記録を達成しました！`,
        id: 1,
        schedule: { at: new Date(Date.now() + 1000) }
      }
    ]
  });
}

// ダークモード対応（OS設定に連動）
import { StatusBar, Style } from '@capacitor/status-bar';

async function setDarkMode(isDark) {
  await StatusBar.setStyle({
    style: isDark ? Style.Dark : Style.Light
  });
}
```

**期待効果**:
- アプリストアでの配信が可能
- 発見性の向上（ストア検索）
- ネイティブ機能の活用
- ユーザーエンゲージメント向上

---

### ステップ3: フルネイティブ化（大規模改修）

**実装時間**: 40-80時間
**コスト**: 高い（開発コスト）
**難易度**: ★★★★★

**メリット**:
- 最高のパフォーマンス
- フルネイティブUI
- 高度なアニメーション
- プラットフォーム固有機能のフル活用

**デメリット**:
- 完全な書き直しが必要
- iOS/Androidで別々の開発が必要（またはクロスプラットフォームフレームワーク使用）
- 保守コストが高い

**技術選択肢**:

#### A. React Native
- **言語**: JavaScript/TypeScript
- **メリット**: JavaScriptベース、既存ロジックを一部流用可能、コミュニティが大きい
- **デメリット**: ブリッジのオーバーヘッド、一部ネイティブコードが必要

#### B. Flutter
- **言語**: Dart
- **メリット**: 美しいUI、Hot Reload開発体験、パフォーマンスが高い
- **デメリット**: Dart言語の学習が必要

#### C. Swift/Kotlin（完全ネイティブ）
- **言語**: Swift（iOS）、Kotlin（Android）
- **メリット**: 最高のパフォーマンス、プラットフォームのすべての機能を活用
- **デメリット**: iOS/Androidで別々の開発が必要、開発コストが高い

**推奨**: 現時点では不要（PWA → Capacitorで十分）

---

## 🗓️ フェーズごとの実装計画

### Phase 1（短期: 1-2週間）

**目標**: ユーザビリティの大幅改善とアプリ化

1. ✅ **Undo機能の実装**（3-4時間）
2. ✅ **ヒント機能の実装**（2-3時間）
3. ✅ **PWA対応**（3-4時間）

**合計**: 8-11時間

**成果物**:
- Undo/Redoが可能なゲーム
- ヒント機能による初心者サポート
- ホーム画面に追加可能なPWAアプリ

---

### Phase 2（中期: 2-4週間）

**目標**: 機能の多様化と没入感の向上

4. ✅ **サウンドエフェクトの実装**（2-3時間）
5. ✅ **設定画面の実装**（4-5時間）
6. ✅ **タイムアタックモードの実装**（3-4時間）

**合計**: 9-12時間

**成果物**:
- サウンド付きゲーム
- カスタマイズ可能な設定
- 複数のゲームモード

---

### Phase 3（長期: 1-2ヶ月）

**目標**: 本格的なアプリとしての展開

7. ✅ **Capacitor化**（6-8時間）
   - iOS/Androidアプリ化
   - ネイティブ機能の統合（振動、シェア）

8. ✅ **リーダーボード実装**（8-10時間）
   - Firebase連携
   - グローバルランキング
   - フレンドランキング

9. ✅ **ソーシャル機能**（4-6時間）
   - スコアシェア（Twitter、LINE、Facebook）
   - スクリーンショット機能
   - SNS連携

**合計**: 18-24時間

**成果物**:
- App Store / Google Playで配信可能なネイティブアプリ
- オンラインランキング機能
- ソーシャル機能によるバイラル効果

---

## 💡 具体的な次のアクション（推奨）

### オプション A: Undo機能の実装
**推定時間**: 3-4時間
**理由**: 最も要望が多そうな機能、パズルゲームの基本

### オプション B: PWA対応
**推定時間**: 3-4時間
**理由**: 最も投資対効果が高い、マーケティング的価値が大きい

### オプション C: Undo + PWA（並行実装） ⭐推奨
**推定時間**: 6-8時間
**理由**: 両方実装することで「本格的なアプリ」として完成度が大幅にアップ

---

## 📊 ROI（投資対効果）分析

| 機能 | 実装時間 | ユーザー価値 | 技術的難易度 | ROI |
|-----|---------|-------------|-------------|-----|
| **Undo機能** | 3-4h | ⭐⭐⭐⭐⭐ | ★★☆☆☆ | **最高** |
| **PWA対応** | 3-4h | ⭐⭐⭐⭐⭐ | ★★☆☆☆ | **最高** |
| **ヒント機能** | 2-3h | ⭐⭐⭐⭐☆ | ★☆☆☆☆ | 高 |
| **サウンド** | 2-3h | ⭐⭐⭐☆☆ | ★★☆☆☆ | 中 |
| **設定画面** | 4-5h | ⭐⭐⭐⭐☆ | ★★★☆☆ | 中〜高 |
| **タイムアタック** | 3-4h | ⭐⭐⭐☆☆ | ★★☆☆☆ | 中 |
| **Capacitor化** | 6-8h | ⭐⭐⭐⭐⭐ | ★★★★☆ | 高 |
| **リーダーボード** | 8-10h | ⭐⭐⭐⭐☆ | ★★★★☆ | 中 |

---

## 🎯 最終推奨

**最優先で実装すべき機能（Phase 1）**:

1. **Undo機能**（3-4時間）
2. **PWA対応**（3-4時間）

この2つを実装すれば：
- 「アプリとしてインストールできる本格パズルゲーム」として完成度が大幅にアップ
- ユーザビリティが劇的に向上
- App Store/Google Playへの移行も容易に
- マーケティング的な訴求力が大きく向上

**合計実装時間**: 6-8時間

---

## 📚 参考リソース

### PWA関連
- [PWA Builder](https://www.pwabuilder.com/) - PWA生成ツール
- [Google PWA Guide](https://web.dev/progressive-web-apps/) - 公式ガイド
- [Workbox](https://developers.google.com/web/tools/workbox) - Service Workerライブラリ

### Capacitor関連
- [Capacitor公式ドキュメント](https://capacitorjs.com/docs)
- [Capacitor Plugins](https://capacitorjs.com/docs/plugins) - 公式プラグイン一覧
- [Ionic Framework](https://ionicframework.com/) - UIコンポーネント（オプション）

### ゲーム開発関連
- [Red Blob Games](https://www.redblobgames.com/) - ゲームアルゴリズム解説
- [Web Audio API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API) - サウンド実装

### Firebase関連（リーダーボード用）
- [Firebase Realtime Database](https://firebase.google.com/docs/database)
- [Firebase Authentication](https://firebase.google.com/docs/auth) - ユーザー認証

---

## 更新履歴

- **2025-10-28**: 初版作成
  - 現状の実装状況を整理
  - 次の実装候補を優先順位付け
  - ネイティブアプリ化の戦略を策定
  - フェーズごとの実装計画を作成
